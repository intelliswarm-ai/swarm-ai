apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-swarmai
  labels:
    app: swarmai
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: swarmai
    release: {{ .Release.Name }}
